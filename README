================
txtserver
================

Is a small text file server a la wiki. The text files are parsed to HTML before they are sent to clients.

Please create the directory 'cached' before starting the server.

An example page is located in data/Test.txt


